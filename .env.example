APP_NAME=DefectMind
APP_ENV=development
DEBUG=true
API_V1_PREFIX=/api

# Database (Phase 2)
# Driver: postgresql+psycopg (psycopg3 async driver)
# Note: URL-encode special characters in password (e.g. @ → %40)
DATABASE_URL=postgresql+psycopg://postgres:YOUR_PASSWORD@127.0.0.1:5432/bugtracker_db
